# Copie para .env (gitignored) e preencha — NUNCA commite valores reais (Princípio V).
# Connection string do Neon (Dashboard -> Connect). Sem ela, o app usa SQLite local.
# DATABASE_URL=postgresql://usuario:senha@ep-xxxx.aws.neon.tech/decisor?sslmode=require
